# PREVIEW ONLY

This branch is an isolated SEO/GEO 2026 preview for Can I Share This?.

Do not merge or promote this branch to production as-is.

The preview `vercel.json` rewrites `/api/*` to the stable production API at `https://canisharethis.com/api/*`. Before production, restore the real API source and production-safe Vercel configuration, run QA, and promote only a validated artifact.
